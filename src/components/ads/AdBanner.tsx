'use client';

import React from 'react';
import type { AdBannerProps } from '@/types/lotto';

// AdSense 자동 광고만 사용 - layout.tsx의 스크립트가 자동으로 광고 배치
const AdBanner: React.FC<AdBannerProps> = () => {
  return null;
};

export default AdBanner;
